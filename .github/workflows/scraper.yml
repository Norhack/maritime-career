name: Scraper
on:
  workflow_dispatch:
  schedule:
    - cron: "0 * * * *"

jobs:
  run-scraper:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Run scraper placeholder"
